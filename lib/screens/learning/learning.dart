import 'package:flutter/material.dart';

class LearningScreens extends StatelessWidget {
  const LearningScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Learning"),
      ),
    );
  }
}
